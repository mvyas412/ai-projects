# Phase 7 unified evaluation v1

This synthetic, public-safe metadata fixture tests the composition layer for parsing,
retrieval, answer outcomes, citation identity, authorization denial, and exact
calculation. It does not replace or expose the protected Phase 5 or Phase 6 suites.

Validation must pass before holdout is evaluated. Required CI makes no provider call.
Candidate result files must preserve case identities and declare provider-call count,
latency, outcome, and evidence identity. Paid or LLM-judged evidence remains opt-in.
