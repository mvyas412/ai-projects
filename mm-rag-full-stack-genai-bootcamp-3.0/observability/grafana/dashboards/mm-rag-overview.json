{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "description": "Request rate grouped only by bounded operation metadata.",
      "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [{"expr": "sum(rate(mm_rag_operations_total[5m])) by (operation, outcome)", "refId": "A"}],
      "title": "Operation rate and outcomes",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "description": "95th-percentile latency by bounded operation name.",
      "fieldConfig": {"defaults": {"unit": "ms"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(mm_rag_operation_duration_milliseconds_bucket[5m])) by (le, operation))", "refId": "A"}],
      "title": "Operation p95 latency",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "tempo", "uid": "tempo"},
      "description": "End-to-end metadata-only request, job, retrieval, and persistence traces.",
      "gridPos": {"h": 9, "w": 24, "x": 0, "y": 8},
      "id": 3,
      "targets": [{"queryType": "traceql", "query": "{ resource.service.name =~ `mm-rag-.*` }", "refId": "A"}],
      "title": "Recent correlated traces",
      "type": "traces"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 41,
  "tags": ["mm-rag", "phase7"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "timezone": "browser",
  "title": "MM-RAG — Product and reliability overview",
  "uid": "mm-rag-overview",
  "version": 1
}
